export type ItemTypes = {
  code: string;
  name: string;
  category: string;
  stack: string;
  quantity: number;
  in: string;
  out?: string;
};

export type CategoryOption = {
  value: string;
  label: string;
};
